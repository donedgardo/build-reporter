{
  "name": "gitlaunch-action",
  "version": "1.0.0",
  "description": "GitHub Action to report builds and deployment status to GitLaunch",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "github-action",
    "deployment",
    "gitlaunch",
    "ci-cd"
  ],
  "author": "GitLaunch",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/http-client": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.0",
    "vitest": "^1.4.0"
  }
}
